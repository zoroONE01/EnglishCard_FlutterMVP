// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility in the flutter_test package. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the constants of widget properties are correct.

import 'package:english_card/app/components/flash_card.dart';
import 'package:english_card/app/my_app.dart';
import 'package:english_card/main.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Counter increments smoke test', (WidgetTester tester) async {
    // Build our app and trigger a frame.


  });
  // group('test group', () {
  //   test('test wordModel', () {
  //     // print(word.toString());
  //
  //   });
  // });
  // testWidgets('finds a specific instance', (WidgetTester tester) async {
  //   await tester.pumpWidget(const MaterialApp(
  //     home: Scaffold(
  //       body: Text('Hello'),
  //     ),
  //   ));
  // });

}
